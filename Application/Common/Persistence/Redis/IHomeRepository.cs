namespace Application.Common.Persistence.Redis;

public interface IHomeRepository
{
    //getTrends();

    //getMenu(data);

    //getHighlight(dataHighlight, dataTimeline, settings);

    //getTimeline(dataTimeline, newsExclusion, settings);

    //getCategoriesBox(data);

    //getLastestNews(data, settings);

    //getCaring(data, settings);

    //getDontForget(data, settings);

    //getNewsByZone(data, settings);

    //getBoxVideo(data, settings);
}
